# Legacy publishing source

이 폴더에는 React 전환 전의 정적 퍼블리싱 원본을 보관합니다.

- `index_H.html`, `index_H.css`: 기존 메인 페이지
- `index_sub.html`, `index_sub.css`: 기존 역사관 페이지
- `js/`, `css/`, `script.js`: 기존 jQuery 및 스타일 의존성

현재 서비스 소스는 프로젝트 루트의 `src/`와 Vite 설정을 사용합니다. 원본 비교가 더 이상 필요 없어질 때까지 이 폴더는 삭제하지 않습니다.
